#!/usr/bin/env sh
set -e

echo "▶ typecheck..."
pnpm run typecheck

echo "▶ lint + format (staged files)..."
pnpm exec lint-staged

echo "▶ unit tests..."
pnpm run test:unit

echo "▶ build..."
pnpm run build

echo "✓ all checks passed"
